#ifndef GAMEOBJECTTYPE_HPP
# define GAMEOBJECTTYPE_HPP

//! enum of all the types of objects in the game
enum eGameObjectType {
	PLAYER,
	ENEMY,
	BOMB,
	WALL,
	SOLIDWALL,
	POWERUP,
	DOOR,
	EXPLOSION
};

#endif